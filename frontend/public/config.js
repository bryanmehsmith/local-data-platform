// Dev-time placeholder for /config.js. In production this file is generated
// at container start by docker-entrypoint.sh from runtime env vars; here it
// just leaves window.__CONFIG__ empty so the app falls back to the
// import.meta.env.VITE_* values (see src/api/client.ts).
window.__CONFIG__ = {};
